package co.edu.unbosque.controller;

import java.util.ArrayList;
import java.util.Scanner;
import java.util.Scanner;

public class OrdenamientoSeleccion {
/*
    public static void main(String[] args) {


        ArrayList ArrayN1 = new ArrayList<Double>();
        ArrayList ArrayNom1 = new ArrayList<Double>();


        ArrayN1[0] = -32;


        ArrayN1.add("FEIJOO GARCIA MIGUEL ALFONSO ");
        ArrayN1.add("RAMIREZ AREVALO HELIO");
        ArrayN1.add("RODRIGUEZ PINTO JAIRO ESTEBAN");
        ArrayN1.add("ROJAS MARTINEZ JUAN JOSE");
        ArrayN1.add("SIERRA ARDILA DIEGO ALEJANDRO");
        ArrayN1.add("PEREZ TORRES DIANA MARCELA");
        ArrayN1.add("PARRA GARCIA MARIA");
        ArrayN1.add("GONZALEZ AREVALO RAMIRO");
        ArrayN1.add("ARIAS FERNANDEZ ANGELICA SOFIA");
        ArrayN1.add("CAMARGO LOPEZ CARLOS JULIO");
        int numeroA = (((String) ArrayNom1.get(1)).codePointAt(1));
        System.out.println(numeroA);

        for(int i=0;i<=9;i++) {
            ArrayN1.add(1.12);
            ArrayN1[i]=ArrayNom1[i].codePointAt(0);
            int numeroA = (((String) ArrayNom1.get(i)).codePointAt(1));
            int numeroB = 100;
            double divisionDecimales = (double)numeroA/numeroB;
            ArrayN1[i]=ArrayN1[i]+divisionDecimales;
            int numeroC = (ArrayNom1[i].codePointAt(2));
            int numeroD = 1000;
            double divisionDecimales2 = (double)numeroC/numeroD;
            ArrayN1[i]=ArrayN1[i]+divisionDecimales2;
            //System.out.println(ArrayN[i]);
        }


        System.out.println("      -SIN ORDENAR-");
        //mostrarNum(ArrayN);
        mostrarNom(ArrayNom);

        System.out.println("       -ORDENADO-   ");
        ordenaSeleccion(ArrayN, ArrayNom);

        System.out.println("       -Invertido-   ");
        ordenaSeleccionInverso(ArrayN, ArrayNom);

    }

    static void ordenaSeleccion(double ArrayN[],String ArrayNom[]) {

        int i, j, pos;
        double menor,tmp;
        String menorn,tmpn;
        for (i = 0; i < ArrayN.length - 1; i++) {
            menor = ArrayN[i];
            menorn = ArrayNom[i];
            pos = i;
            for (j = i + 1; j < ArrayN.length; j++){
                if (ArrayN[j] < menor) {
                    menor = ArrayN[j];
                    menorn = ArrayNom[j];
                    pos = j;
                }
            }
            if (pos != i){
                tmp = ArrayN[i];
                ArrayN[i] = ArrayN[pos];
                ArrayN[pos] = tmp;

                tmpn= ArrayNom[i];
                ArrayNom[i] = ArrayNom[pos];
                ArrayNom[pos] = tmpn;
            }
        }
        //mostrarNum(ArrayN);
        mostrarNom(ArrayNom);
    }

    static void ordenaSeleccionInverso(double ArrayN[],String ArrayNom[]) {

        int i, j, pos;
        double mayor,tmp;
        String mayorn,tmpn;
        for (i = 0; i < ArrayN.length - 1; i++) {
            mayor = ArrayN[i];
            mayorn = ArrayNom[i];
            pos = i;
            for (j = i + 1; j < ArrayN.length; j++){
                if (ArrayN[j] > mayor) {
                    mayor = ArrayN[j];
                    mayorn = ArrayNom[j];
                    pos = j;
                }
            }
            if (pos != i){
                tmp = ArrayN[i];
                ArrayN[i] = ArrayN[pos];
                ArrayN[pos] = tmp;

                tmpn= ArrayNom[i];
                ArrayNom[i] = ArrayNom[pos];
                ArrayNom[pos] = tmpn;
            }
        }
        //mostrarNum(ArrayN);
        mostrarNom(ArrayNom);
    }


    static void mostrarNum(double ArrayN[]) {

        for (int i = 0; i < ArrayN.length; i++) {
            System.out.print(" Elemento " + (i + 1) + " -----> " + ArrayN[i] + "\n");
        }

    }
    static void mostrarNom(String ArrayNom[]) {

        for (int i = 0; i < ArrayNom.length; i++) {
            System.out.print(" Elemento " + (i + 1) + " -----> " + ArrayNom[i] + "\n");
        }

    }
*/

}

